\ifdefined\ishandout
    \documentclass[handout]{beamer}
    \else
    % \documentclass[handout]{beamer}
    \documentclass{beamer}
    % \setbeamercovered{transparent}
\fi

\includeonly{latex}

\usepackage{selthcsslides}
\usecolortheme{lucolors}
\usepackage{latex_utils}

\usepackage{beamerutil}

% The Madrid theme, for some reason, does not show bullet points in list
% environments. This is a workaround to fix that.
\setbeamertemplate{itemize item}[default]
\setbeamertemplate{itemize subitem}[default]
\setbeamertemplate{enumerate item}[default]
\setbeamertemplate{enumerate subitem}[default]

% Beamer uses letters for footnotes by default. Change to numbers.
% (Doesn't seem to work, though. I'll leave it for now.)
\setbeamertemplate{footnote}{\insertfootnotemark}

% Number captions
\setbeamertemplate{caption}[numbered]

% Adjust size of verbatim examples
\usepackage{fancyvrb}
\fvset{tabsize=2, fontsize=\scriptsize}


% \usepackage[swedish]{babel}
% Swedish setup using polyglossia for better multilingual support
\usepackage{polyglossia}
\setmainlanguage{swedish}

\usepackage{url}
\usepackage{datetime2}
\usepackage{xcolor}

% \usepackage{fontspec}  			% Allows loading system fonts
% \setmainfont{Latin Modern Roman}  % Set the main (serif) font
% \setsansfont{Latin Modern Roman}  % Force sans-serif font to use the same font

\author[]{Mattias Nordahl}
\institute{\url{mattias.nordahl@cs.lth.se}}
\date{}

\usepackage{tikz}
% For environment overlays
\newenvironment{envi}{%
  \begin{onlyenv}<+->
}{%
  \end{onlyenv}
}
\newenvironment{envs}{%
  \begin{onlyenv}<.->
}{%
  \end{onlyenv}
}

%***************************************************************

\begin{document}

\title{Datorer och datoranvändning\\Föreläsning 3 --- \LaTeX}

\frame[plain]{
    \maketitle
    \vspace{-2\baselineskip}
}

\date{\the\year/\the\numexpr\year-1999}

% Include actual slides
\include{latex}

\end{document}
